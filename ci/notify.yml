---
platform: linux

inputs:
- name: prometheus-config

outputs:
- name: email

run:
  path: prometheus-config/ci/notify.sh

params:
  ALERT_MESSAGE:
  ALERT_SOURCE:
  ALERT_SEVERITY:
  ALERT_COMPONENT:
