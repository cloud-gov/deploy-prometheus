---
platform: linux

inputs:
- {name: prometheus-config}

run:
  path: prometheus-config/ci/cdn-broker-certs.sh

params:
  AWS_DEFAULT_REGION:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  ENVIRONMENT:
  GATEWAY_HOST:
